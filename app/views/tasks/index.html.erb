<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
  <li>
    <p><%= link_to "Task No.#{task.id}:", task %> <%= task.content %></p>
    <% end %>
  </li>
</ul>

<%=link_to "新規タスクの登録", new_task_path %>